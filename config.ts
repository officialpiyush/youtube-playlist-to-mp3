var config = {
    playlist: "", //Enter Your Playlist URL Here
    //autoDownload: true // Boolean: if u dont want the program to download the mp3 files through the program, set it as false
}

export default config